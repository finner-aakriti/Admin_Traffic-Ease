<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aakriti.trafficease">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_admin"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_admin"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrafficEase">
        <activity
            android:name=".report_activity"
            android:exported="false"
            android:label="@string/title_activity_report"
            android:theme="@style/Theme.TrafficEase.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>